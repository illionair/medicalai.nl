{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -Command \"if (Get-Command graphify -ErrorAction SilentlyContinue) { graphify hook-check; exit $LASTEXITCODE } else { exit 0 }\""
          }
        ]
      }
    ]
  }
}
